export type NotificationSettingsProps = {

}
export default function NotificationSettings({ }: NotificationSettingsProps) {
    // method: e-mail, device
    // kind: new message / new match / new like
    return (
        <div>
        </div>
    )
}